# Copy to `.env` in this directory (next to docker-compose.yml) for production deploy.

DATABASE_URL=postgresql://user:password@host:5432/dbname
CORS_ORIGINS=https://finance.example.com
DEMO_MODE=false
HTTP_PORT=8080
